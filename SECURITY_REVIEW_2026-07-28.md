# MUSICIAN本番セキュリティ確認

確認日: 2026-07-28

## 実施済み

- `.git`、`.env`、設定PHP、旧WordPress入口、旧バックアップ、サンプル、診断用URLを外部公開しないルールを追加。
- ディレクトリ一覧を無効化。
- `X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy`、`Permissions-Policy`を本番確認。
- `X-Powered-By`を非表示化。
- Git管理対象にFTPパスワード、APIキー、秘密鍵が含まれていないことを確認。
- GitHub公開成功後だけ、一時アップロード、旧ロールバックコピー、指定済み旧CMS残骸を削除する処理を追加。
- 公開後の検証に失敗した場合は、直前の本番へ自動復元する処理を追加。

## ホスティング契約側で対応が必要

### 最優先: SSL証明書

`www.musician.co.jp`の証明書は2026-05-11で期限切れ。PlanManagerには更新操作がなく、
GMOクラウドのアカウントマネージャーから無料のアルファSSLを更新申請する必要がある。
アカウントマネージャーの認証情報が必要なため、サイトファイルだけでは更新できない。

更新後に行うこと:

1. 通常の証明書検証でHTTPSが成功することを確認。
2. `musician.co.jp`と`www.musician.co.jp`の両方を確認。
3. 有効期限を確認後、HSTSの導入可否を再検討。

### PHP

本番はサポート終了済みのPHP 7.4。管理画面ではPHP 8.4へ変更できるが、現在のCakePHP系
アプリケーションとの互換性を確認せず本番だけ切り替えると、全ページや管理画面が停止する
可能性がある。別環境でPHP 8.4対応を行ってから切り替える。

### 管理画面の二段階認証

PlanManagerの二段階認証は未設定。本人の認証端末が必要なため自動設定は行わない。
SSL更新とあわせて設定する。

## 検査結果

- サイトマップ掲載ページ: 36 / 36 正常
- 内部画像: 295 / 295 正常
- 内部リンク: 84 / 84 正常
- 各ページのH1、title、canonical: 正常
- 画像alt欠落: 0
- Achievements: 2006〜2026、全21年、日付なし旧実績2,672件を確認
